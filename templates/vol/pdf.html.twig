<!DOCTYPE html>
<html lang="fr">
<head>
    <style>
        body {
            font-family: helvetica;
            font-size: 12px;
        }
        .header {
            width: 100%;
            border-bottom: 1px solid #ccc;
            padding-bottom: 10px;
            margin-bottom: 20px;
        }
        .logo {
            width: 120px;
            height: auto;
        }
        h1 {
            text-align: center;
            font-size: 18px;
            margin-bottom: 20px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            margin-bottom: 20px;
        }
        table th {
            background-color: #f2f2f2;
            font-weight: bold;
            border: 1px solid #000;
            padding: 8px;
            text-align: left;
        }
        table td {
            border: 1px solid #000;
            padding: 8px;
            text-align: left;
        }
        .signature-container {
            width: 100%;
            text-align: right;
            margin-top: 30px;
        }
        .signature-image {
            width: 150px;
            height: auto;
        }
        .signature-text {
            font-style: italic;
            margin-top: 5px;
        }
        .qr-code {
            width: 80px;
        }
        .date-info {
            margin-bottom: 15px;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="header">
        <img src="{{ app.request.schemeAndHttpHost ~ asset('images/logo.png') }}" class="logo" />
    </div>

    <h1>Liste des Vols de {{ voyage.depart }} à {{ voyage.destination }}</h1>
    
    <div class="date-info">
        Date d'exportation: {{ "now"|date('d/m/Y') }}
    </div>

    <table>
        <thead>
            <tr>
                <th>Airline</th>
                <th>Flight Number</th>
                <th>Departure</th>
                <th>Arrival</th>
                <th>Price (DT)</th>
            </tr>
        </thead>
        <tbody>
            {% for flight in flights %}
                <tr>
                    <td>{{ flight.airline }}</td>
                    <td>{{ flight.flightNumber }}</td>
                    <td>{{ flight.departureTime|date('Y-m-d H:i') }}</td>
                    <td>{{ flight.arrivalTime|date('Y-m-d H:i') }}</td>
                    <td>{{ flight.price }} DT</td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="5" style="text-align: center;">Aucun vol disponible</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <div class="signature-container">
        <img src="{{ app.request.schemeAndHttpHost ~ asset('images/signature.png') }}" class="signature-image" />
        <p class="signature-text">Liste Des Vols Disponible Le {{ "now"|date('d/m/Y') }}</p>
    </div>
</body>
</html>