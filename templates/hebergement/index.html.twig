{% extends 'base_hebergement.html.twig' %}
{% block title %}Liste des hébergements{% endblock %}

{% block body %}
 
<header>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script> {# Ajout crucial #}
</header>
<nav class="navbar navbar-expand-lg navbar-custom">
    <div class="container-fluid">
<form class="d-flex align-items-center gap-2" method="GET" id="searchForm">
    <div class="row g-2 w-100 flex-nowrap">
                <!-- Filtre Type -->
                        <div class="col-6 col-md-3">

            <select class="form-select form-select-sm auto-submit" name="type">
    <option value="">Tous les types</option>
    <option value="hotel" {{ currentFilters.type == 'hotel' ? 'selected' }}>Hôtel</option>
    <option value="event" {{ currentFilters.type == 'event' ? 'selected' }}>Hostel</option>
    <option value="travel" {{ currentFilters.type == 'travel' ? 'selected' }}>Maison</option>
</select>
</div>
                <!-- Tri par Prix -->
        <div class="col-6 col-md-3">
            <select class="form-select form-select-sm auto-submit" name="sortPrice" id="sortPrice">
    <option value="">Trier par prix</option>
    <option value="asc" {{ currentFilters.sortPrice == 'asc' ? 'selected' }}>Prix croissant</option>
    <option value="desc" {{ currentFilters.sortPrice == 'desc' ? 'selected' }}>Prix décroissant</option>
</select>
        </div>
                <!-- Filtre Adresse -->
                 <div class="col-6 col-md-3">
            <input type="text" 
                   class="form-control form-control-sm auto-submit" 
                   name="address" 
                   placeholder="Adresse" 
                   value="{{ currentFilters.address }}">
        </div>

                <!-- Recherche par Nom -->
                 <div class="col-6 col-md-3">
            <div class="input-group input-group-sm">
                <input type="text" 
                       class="form-control auto-submit" 
                       name="searchTerm" 
                       placeholder="Rechercher par nom..."
                       value="{{ currentFilters.searchTerm }}">
                <button class="btn btn-primary" type="submit">
                    <i class="bi bi-search"></i>
                </button>
            </div>
        </div>
            </div>
        </form>
    </div>
</nav>
<div class="container my-5">
    <h1 class="mb-4 text-center display-5 fw-bold text-dark mb-5">Nos Hébergements Exceptionnels</h1>
    
<div class="row mb-5">
<div class="d-flex justify-content-between align-items-center mb-5">
      
        <a href="{{ path('app_hebergement_new') }}" class="btn btn-primary btn-lg rounded-pill shadow-sm">
            <i class="bi bi-plus-circle me-2"></i>Ajouter un hébergement
        </a>
         <a href="{{ path('app_hebergement_export_pdf') }}" 
           class="btn btn-danger btn-lg rounded-pill shadow-sm">
            <i class="bi bi-file-earmark-pdf me-2"></i>Exporter PDF
        </a>
    </div>
    
        <!-- Cartes Statistiques -->
     <div class="col-lg-4">
            <div class="row g-4">
                <div class="col-12">
                    <div class="stat-card shadow-sm border-0 h-100">
                        <div class="card-body p-4">
                            <div class="d-flex align-items-center gap-3">
                                <div class="icon-stat bg-primary-soft rounded-circle p-3">
                                    <i class="bi bi-house-door fs-3"></i>
                                </div>
                                <div>
                                    <div class="stat-label">Total Hébergements</div>
                                    <div class="stat-value">{{ hebergements|length }}</div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="col-md-6 col-lg-12">
                    <div class="stat-card shadow-sm border-0 h-100">
                        <div class="card-body p-4">
                            <div class="d-flex align-items-center gap-3">
                                <div class="icon-stat bg-success-soft rounded-circle p-3">
                                    <i class="bi bi-currency-euro fs-3"></i>
                                </div>
                                <div>
                                    <div class="stat-label">Prix Moyen</div>
                                    <div class="stat-value price-value">
                                        {{ stats.price_stats.avg_price|round }} €
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="col-md-6 col-lg-12">
                    <div class="stat-card shadow-sm border-0 h-100">
                        <div class="card-body p-4">
                            <div class="d-flex align-items-center gap-3">
                                <div class="icon-stat bg-info-soft rounded-circle p-3">
                                    <i class="bi bi-bar-chart fs-3"></i>
                                </div>
                                <div>
                                    <div class="stat-label">Étendue des Prix</div>
                                    <div class="stat-value range-value">
                                        {{ stats.price_stats.min_price }} € - {{ stats.price_stats.max_price }} €
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Graphiques -->
        <div class="col-lg-8">
            <div class="row g-4">
                <div class="col-12">
                    <div class="card shadow-sm border-0">
                        <div class="card-body p-4">
                            <h5 class="card-title mb-4">Répartition par Type</h5>
                            <div class="chart-container">
                                <canvas id="typeChart"></canvas>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="col-12">
                    <div class="card shadow-sm border-0">
                        <div class="card-body p-4">
                            <h5 class="card-title mb-4">Prix Moyen par Type</h5>
                            <div class="chart-container">
                                <canvas id="priceChart"></canvas>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    {% if hebergements is empty %}
        <div class="alert alert-info text-center rounded-pill">Aucun hébergement disponible actuellement</div>
    {% else %}
        <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
            {% for hebergement in hebergements %}
                <div class="col">
                    <div class="card h-100 border-0 shadow-lg-hover">
                        <div class="card-header bg-gradient-primary text-white rounded-top-3 py-4">
                            <h5 class="card-title mb-0 text-center">{{ hebergement.nom }}</h5>
                        </div>
                        
                        <div class="card-body position-relative">
                            <div class="position-absolute top-0 start-50 translate-middle mt-n3">
                                <span class="badge bg-white text-dark border border-2 border-primary shadow-sm px-3 py-2">
                                    {{ hebergement.type }}
                                </span>
                            </div>

                            <div class="mb-4 pt-4">
                                <p class="card-text text-dark mb-3">
                                    <i class="bi bi-geo-alt-fill text-primary me-2"></i>
                                    {{ hebergement.adresse }}
                                </p>

                                <p class="card-text text-secondary mb-4 line-clamp-3">
                                    {{ hebergement.description }}
                                </p>

                                <div class="d-flex justify-content-between align-items-center">
                                    <div class="h3 text-success mb-0">
                                        {{ hebergement.prix }}<small class="fs-6 text-muted">€/nuit</small>
                                    </div>
                                    <a href="{{ path('app_hebergement_show', {'id': hebergement.id}) }}" 
                                       class="btn btn-link text-decoration-none text-primary p-0">
                                        Voir détails <i class="bi bi-arrow-right-short"></i>
                                    </a>
                                </div>
                            </div>
                        </div>

                        <div class="card-footer bg-transparent border-top-0 text-end py-3">
                            <a href="{{ path('app_hebergement_edit', {'id': hebergement.id}) }}" 
                               class="btn btn-sm btn-outline-secondary rounded-pill px-3">
                                <i class="bi bi-pencil-square me-2"></i>Modifier
                            </a>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    {% endif %}
</div>

<style>
/* Navbar principale */

/* Éléments de formulaire */
.navbar-custom .form-select-sm {
    border-radius: 60px !important;
    padding: 0.5rem 1.25rem;
    border: 2px solid #dee2e6;
    transition: all 0.3s ease;
}
.navbar-custom .row.flex-nowrap {
    overflow-x: auto; /* Permet le défilement horizontal sur mobile */
    scrollbar-width: none; /* Masque la scrollbar sur Firefox */
    -ms-overflow-style: none; /* Masque la scrollbar sur IE/Edge */
}

.navbar-custom .row.flex-nowrap::-webkit-scrollbar {
    display: none; /* Masque la scrollbar sur Chrome/Safari */
}

.navbar-custom .form-select-sm {
    min-width: 120px; /* Largeur minimale pour les selects */
}
/* Dans la section style existante */
.form-control-sm {
    border-radius: 60px !important;
    padding: 0.5rem 1.25rem;
    border: 2px solid #dee2e6;
    transition: all 0.3s ease;
}

.input-group-sm .form-control {
    border-radius: 60px 0 0 60px !important;
    border-right: none;
}

.input-group-sm .btn {
    border-radius: 0 60px 60px 0 !important;
    padding: 0.5rem 1.5rem;
}

input[type="number"]::-webkit-inner-spin-button,
input[type="number"]::-webkit-outer-spin-button {
    -webkit-appearance: none;
    margin: 0;
}

input[type="number"] {
    -moz-appearance: textfield;
}
@media (min-width: 768px) {
    .navbar-custom .form-select-sm {
        min-width: auto; /* Réinitialise sur desktop */
    }
    
    .navbar-custom .input-group-sm {
        min-width: 250px; /* Largeur minimale pour la recherche */
    }
}
.navbar-custom .input-group-sm .form-control {
    border-radius: 60px 0 0 60px !important;
    padding: 0.5rem 1.5rem;
    border-right: none;
}
/* Ajustements pour les selects */
.navbar-custom select.form-select-sm {
    -webkit-appearance: none;
    -moz-appearance: none;
    appearance: none;
    background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 16 16'%3e%3cpath fill='none' stroke='%23343a40' stroke-linecap='round' stroke-linejoin='round' stroke-width='2' d='M2 5l6 6 6-6'/%3e%3c/svg%3e");
    background-repeat: no-repeat;
    background-position: right 1rem center;
    background-size: 16px 12px;
}
/* Hover states */
.navbar-custom .form-select-sm:hover,
.navbar-custom .input-group-sm .form-control:hover {
    border-color: #3b82f6;
}
/* Focus states */
.navbar-custom .form-select-sm:focus,
.navbar-custom .input-group-sm .form-control:focus {
    box-shadow: 0 0 0 0.25rem rgba(59, 130, 246, 0.25);
    border-color: #3b82f6;
}
/* Boutons */
.navbar-custom .btn {
    padding: 0.375rem 0.75rem;
    line-height: 1.5;
}

/* Responsive */
@media (max-width: 992px) {
    .main-header {
        padding: 0.5rem 1rem;
    }
    
    .navbar-custom .form-control,
    .navbar-custom .form-select {
        margin: 0.25rem 0;
    }
}

/* Alignement vertical */
.navbar-custom {
    align-items: flex-start;
}

@media (min-width: 992px) {
    .navbar-custom {
        align-items: center;
    }
}

/* Recherche */
.input-group-sm .btn-primary {
    padding-left: 1rem;
    padding-right: 1rem;
}
/* Améliorations des statistiques */
    .icon-stat {
        width: 60px;
        height: 60px;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .bg-primary-soft { background-color: rgba(59, 130, 246, 0.1) }
    .bg-success-soft { background-color: rgba(16, 185, 129, 0.1) }
    .bg-info-soft { background-color: rgba(6, 182, 212, 0.1) }

    /* Améliorations des cartes */
    .card {
        border-radius: 1rem;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 20px rgba(0, 0, 0, 0.08);
    }

    /* Ajustements des graphiques */
    .chart-container {
        height: 300px;
        position: relative;
    }

    @media (max-width: 768px) {
        .chart-container {
            height: 250px;
        }
    }

    /* Améliorations du bouton */
    .btn-primary {
        background: linear-gradient(135deg, #3b82f6 0%, #6366f1 100%);
        border: none;
        padding: 0.75rem 1.5rem;
    }

    .btn-primary:hover {
        background: linear-gradient(135deg, #2563eb 0%, #4f46e5 100%);
    }
    .card {
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        border-radius: 1rem;
        overflow: hidden;
        background: #ffffff;
        border: 1px solid rgba(228, 231, 235, 0.5);
    }
    
    .card:hover {
        transform: translateY(-0.5rem);
        box-shadow: 0 12px 24px -6px rgba(0, 0, 0, 0.1);
    }
    
    .bg-gradient-primary {
        background: linear-gradient(135deg, #3b82f6 0%, #6366f1 100%);
    }
    
    .line-clamp-3 {
        display: -webkit-box;
        -webkit-line-clamp: 3;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }
     /* Ajoutez ceci à votre section style existante */
    .chart-container {
        position: relative;
        height: 100%;
        min-height: 100px;
    }
    
    /* Responsive des cartes statistiques */
    @media (max-width: 992px) {
        .stats-column {
            order: -1;
            margin-bottom: 2rem;
        }
    }
    .badge {
        font-weight: 600;
        letter-spacing: 0.5px;
        text-transform: uppercase;
        font-size: 0.75rem;
    }
    
    .shadow-lg-hover {
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
    }
    
    .card:hover .shadow-lg-hover {
        box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.08);
    }
    
    .rounded-top-3 {
        border-top-left-radius: 1rem !important;
        border-top-right-radius: 1rem !important;
    }
     .btn-danger {
        background: linear-gradient(135deg, #dc3545 0%, #bb2d3b 100%);
        border: none;
        transition: all 0.3s ease;
    }
    
    .btn-danger:hover {
        background: linear-gradient(135deg, #bb2d3b 0%, #9a2530 100%);
        transform: translateY(-2px);
    }
    
    .btn-group {
        gap: 0.5rem;
    }
    
    @media (max-width: 768px) {
        .btn-group {
            width: 100%;
            flex-direction: column;
        }
        
        .btn-lg {
            width: 100% !important;
        }
    }

    /* Styles de base */
    .accommodation-card {
        background: var(--card-bg);
        border-radius: 15px;
        overflow: hidden;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        border: 1px solid var(--border-color);
        height: 100%;
    }

    .accommodation-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
    }

    .card-header {
        background: var(--navy-blue);
        color: white;
        padding: 1.5rem;
        border-bottom: none;
    }

    .card-body {
        padding: 1.5rem;
        background: var(--card-bg);
    }

    .badge {
        font-size: 0.8rem;
        padding: 0.5rem 1rem;
        border-radius: 20px;
        font-weight: 500;
    }

    .price {
        font-size: 1.5rem;
        color: #28a745;
        font-weight: bold;
    }

    .location {
        color: var(--text-secondary);
        font-size: 0.9rem;
        margin-bottom: 1rem;
    }

    .location i {
        color: var(--navy-blue);
    }

    .btn-details {
        color: var(--navy-blue);
        text-decoration: none;
        font-weight: 500;
        transition: color 0.3s ease;
    }

    .btn-details:hover {
        color: var(--navy-blue-dark);
    }

    .btn-edit {
        border: 1px solid var(--border-color);
        background: transparent;
        color: var(--text-primary);
        padding: 0.5rem 1rem;
        border-radius: 20px;
        transition: all 0.3s ease;
    }

    .btn-edit:hover {
        background: var(--hover-bg);
        color: var(--text-primary);
    }

    /* Style pour le graphique */
    .chart-container {
        background: var(--card-bg);
        border-radius: 15px;
        padding: 1.5rem;
        margin-bottom: 2rem;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    [data-theme="dark"] .chart-container {
        background: var(--card-bg);
        border: 1px solid var(--border-color);
    }

    [data-theme="dark"] .accommodation-card {
        background: var(--card-bg);
        border-color: var(--border-color);
    }

    [data-theme="dark"] .card-body {
        background: var(--card-bg);
        color: var(--text-primary);
    }

    [data-theme="dark"] .location {
        color: var(--text-secondary);
    }

    [data-theme="dark"] .btn-edit {
        border-color: var(--border-color);
        color: var(--text-primary);
    }

    [data-theme="dark"] .btn-edit:hover {
        background: var(--hover-bg);
    }

    [data-theme="dark"] .price {
        color: #2ecc71;
    }

    /* Responsive design */
    @media (max-width: 768px) {
        .accommodation-card {
            margin-bottom: 1rem;
        }
        
        .chart-container {
            padding: 1rem;
        }
    }

    /* Styles pour le mode sombre */
    [data-theme="dark"] {
        --text-color: #ffffff;
        --text-muted: rgba(255, 255, 255, 0.7);
        --card-text: #ffffff;
    }

    [data-theme="dark"] h1,
    [data-theme="dark"] h2,
    [data-theme="dark"] h3,
    [data-theme="dark"] h4,
    [data-theme="dark"] h5,
    [data-theme="dark"] h6,
    [data-theme="dark"] .text-dark {
        color: var(--text-color) !important;
    }

    [data-theme="dark"] .text-muted {
        color: var(--text-muted) !important;
    }

    [data-theme="dark"] .card {
        background: var(--card-bg);
        border-color: var(--border-color);
    }

    [data-theme="dark"] .card-body {
        color: var(--card-text);
    }

    [data-theme="dark"] .card-text {
        color: var(--text-color);
    }

    [data-theme="dark"] .text-secondary {
        color: var(--text-muted) !important;
    }

    [data-theme="dark"] .form-select,
    [data-theme="dark"] .form-control {
        background-color: var(--input-bg);
        color: var(--text-color);
        border-color: var(--border-color);
    }

    [data-theme="dark"] .form-select option {
        background-color: var(--card-bg);
        color: var(--text-color);
    }

    [data-theme="dark"] .chart-container {
        background: var(--card-bg);
    }

    [data-theme="dark"] .chart-container h5 {
        color: var(--text-color);
    }

    /* Style des statistiques en mode sombre */
    [data-theme="dark"] .icon-stat {
        background-color: rgba(255, 255, 255, 0.1);
    }

    [data-theme="dark"] .text-primary {
        color: #60a5fa !important;
    }

    [data-theme="dark"] .text-success {
        color: #34d399 !important;
    }

    [data-theme="dark"] .text-info {
        color: #67e8f9 !important;
    }

    /* Style des badges en mode sombre */
    [data-theme="dark"] .badge.bg-white {
        background-color: var(--card-bg) !important;
        color: var(--text-color) !important;
        border-color: rgba(255, 255, 255, 0.2);
    }

    /* Style des boutons en mode sombre */
    [data-theme="dark"] .btn-outline-secondary {
        color: var(--text-color);
        border-color: var(--border-color);
    }

    [data-theme="dark"] .btn-outline-secondary:hover {
        background-color: rgba(255, 255, 255, 0.1);
        color: var(--text-color);
    }

    /* Style du graphique en mode sombre */
    [data-theme="dark"] #typeChart,
    [data-theme="dark"] #priceChart {
        color: var(--text-color);
    }

    /* Styles des statistiques en mode sombre */
    [data-theme="dark"] .stats-section {
        color: var(--text-color);
    }

    [data-theme="dark"] .icon-stat {
        background-color: rgba(255, 255, 255, 0.1);
    }

    [data-theme="dark"] .icon-stat i {
        color: var(--text-color) !important;
    }

    [data-theme="dark"] .stat-value {
        color: var(--text-color) !important;
        font-size: 2rem;
        font-weight: bold;
    }

    [data-theme="dark"] .stat-label {
        color: var(--text-muted);
        font-size: 0.9rem;
        margin-bottom: 0.5rem;
    }

    [data-theme="dark"] .stat-card {
        background: var(--card-bg);
        border: 1px solid var(--border-color);
    }

    [data-theme="dark"] .stat-card h2,
    [data-theme="dark"] .stat-card h4,
    [data-theme="dark"] .stat-card .text-primary,
    [data-theme="dark"] .stat-card .text-success,
    [data-theme="dark"] .stat-card .text-info,
    [data-theme="dark"] .stat-card .text-dark {
        color: var(--text-color) !important;
    }

    [data-theme="dark"] .stat-card .text-muted {
        color: var(--text-muted) !important;
    }

    /* Style des icônes dans les stats */
    [data-theme="dark"] .icon-stat.bg-primary-soft i {
        color: #60a5fa !important;
    }

    [data-theme="dark"] .icon-stat.bg-success-soft i {
        color: #34d399 !important;
    }

    [data-theme="dark"] .icon-stat.bg-info-soft i {
        color: #67e8f9 !important;
    }

    /* Style des valeurs statistiques */
    [data-theme="dark"] .price-value {
        color: #34d399 !important;
    }

    [data-theme="dark"] .range-value {
        color: var(--text-color) !important;
    }

    /* Mise à jour du HTML des statistiques */
    .stat-card {
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .stat-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
    }
</style>
<script>
document.addEventListener('DOMContentLoaded', function() {
    // Auto-submit des filtres
    document.querySelectorAll('.auto-submit').forEach(element => {
        element.addEventListener('change', function() {
            document.getElementById('searchForm').submit();
        });
    });

    // Gestion du délai pour la recherche texte
    let searchTimeout;
    document.querySelector('input[name="searchTerm"]').addEventListener('input', function(e) {
        clearTimeout(searchTimeout);
        searchTimeout = setTimeout(() => {
            document.getElementById('searchForm').submit();
        }, 500);
    });

    const isDarkMode = document.documentElement.getAttribute('data-theme') === 'dark';
    const textColor = isDarkMode ? '#ffffff' : '#333333';
    const gridColor = isDarkMode ? 'rgba(255, 255, 255, 0.1)' : 'rgba(0, 0, 0, 0.1)';

    // Configuration du graphique circulaire
    const typeChart = new Chart(document.getElementById('typeChart'), {
        type: 'doughnut',
        data: {
            labels: {{ stats.count_by_type|map(t => t.type)|json_encode|raw }},
            datasets: [{
                data: {{ stats.count_by_type|map(t => t.count)|json_encode|raw }},
                backgroundColor: [
                    '#60a5fa', '#34d399', '#67e8f9', '#fbbf24', '#ef4444'
                ],
                borderWidth: 0,
                hoverOffset: 10
            }]
        },
        options: {
            maintainAspectRatio: false,
            plugins: {
                legend: {
                    position: 'right',
                    labels: {
                        color: textColor,
                        boxWidth: 20,
                        padding: 15,
                        font: { size: 14 }
                    }
                },
                tooltip: {
                    bodyFont: { size: 14 },
                    titleFont: { size: 16 }
                }
            }
        }
    });

    // Configuration du graphique à barres
    const priceChart = new Chart(document.getElementById('priceChart'), {
        type: 'bar',
        data: {
            labels: {{ stats.avg_price_by_type|map(t => t.type)|json_encode|raw }},
            datasets: [{
                label: 'Prix Moyen (€)',
                data: {{ stats.avg_price_by_type|map(t => t.average_price)|json_encode|raw }},
                backgroundColor: '#60a5fa',
                borderRadius: 8,
                borderSkipped: false
            }]
        },
        options: {
            maintainAspectRatio: false,
            responsive: true,
            scales: {
                y: {
                    beginAtZero: true,
                    grid: { 
                        color: gridColor,
                        drawBorder: false
                    },
                    ticks: {
                        color: textColor,
                        callback: value => value + ' €',
                        font: { size: 14 }
                    }
                },
                x: {
                    grid: { 
                        display: false 
                    },
                    ticks: {
                        color: textColor,
                        font: { size: 14 }
                    }
                }
            },
            plugins: {
                legend: {
                    display: false
                },
                tooltip: {
                    bodyFont: { size: 14 },
                    titleFont: { size: 16 }
                }
            }
        }
    });
});
</script>
{% endblock %}