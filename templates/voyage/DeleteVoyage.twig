<form method="post" action="{{ path('app_voyage_delete', {'VID': voyage.VID}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ voyage.VID) }}">
    <button class="btn">DELETE</button>
</form>
